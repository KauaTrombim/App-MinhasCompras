<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MinhasCompras.Views.ListaProduto"
             Title="Minhas Compras">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Somar" Clicked="ToolbarItem_Clicked_1"/>
        <ToolbarItem Text="Adicionar" Clicked="ToolbarItem_Clicked"/>
    </ContentPage.ToolbarItems>

    <VerticalStackLayout>
        <SearchBar x:Name="txt_search"
                   Placeholder="Buscar Produtos"
                   TextChanged="txt_search_TextChanged"/>

        <CollectionView x:Name="lst_produtos" ItemsSource="{Binding listaFiltrada}">
            <CollectionView.Header>
                <Grid ColumnDefinitions="*, *, *, *, *">
                    <Label Grid.Column="0" Text="ID"/>
                    <Label Grid.Column="1" Text="Descrição"/>
                    <Label Grid.Column="2" Text="Preço"/>
                    <Label Grid.Column="3" Text="Qnt"/>
                    <Label Grid.Column="4" Text="Total"/>
                </Grid>
            </CollectionView.Header>

            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid ColumnDefinitions="*, *, *, *, *">
                        <Grid.GestureRecognizers>
                            <TapGestureRecognizer Tapped="MenuItem_Clicked" />
                        </Grid.GestureRecognizers>

                        <Label Grid.Column="0" Text="{Binding Id}"/>
                        <Label Grid.Column="1" Text="{Binding Descricao}"/>
                        <Label Grid.Column="2" Text="{Binding Preco, StringFormat='R$ {0:F2}'}"/>
                        <Label Grid.Column="3" Text="{Binding Quantidade}"/>
                        <Label Grid.Column="4" Text="{Binding Total, StringFormat='R$ {0:F2}'}"/>
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>
</ContentPage>
